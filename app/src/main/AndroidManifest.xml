<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.glemmekasse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.EditGroup"></activity>
        <activity android:name=".views.YourGroup" />
        <activity android:name=".views.ChangePassword" />
        <activity android:name=".views.CreatePosts" />
        <activity android:name=".views.CreateGroup" />
        <activity android:name=".views.forget_password" />
        <activity android:name=".views.SignUpActivity" />
        <activity android:name=".views.LoginActivity" />
        <activity android:name=".views.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>